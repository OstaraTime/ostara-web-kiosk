server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location /kiosk/assets/ {
        try_files $uri =404;
    }

    location /kiosk/ {
        try_files $uri /kiosk/index.html;
    }

}
